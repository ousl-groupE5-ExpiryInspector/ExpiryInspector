import React from 'react';
import { View, Text } from 'react-native';

export default function InventoryExpired({ navigation }) {
  return (
    <View>
      <Text>Epired screen</Text>
    </View>
  );
}